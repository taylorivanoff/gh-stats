[package]
name = "gh-stats-cli"
version.workspace = true
description = "CLI for GhStats — fetch, export, and serve GitHub portfolio analytics"
authors.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gh-stats-core = { path = "../gh-stats-core" }
serde_json = "1"
tiny_http = "0.12"
